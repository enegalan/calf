name: ui
description: "Calf UI App."
publish_to: 'none'
version: 0.9.5+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  shadcn_ui: ^0.55.0
  web_socket_channel: ^3.0.3
  xterm: ^4.0.0
  fl_chart: ^0.70.2
  path: ^1.9.1
  path_provider: ^2.1.5
  file_selector: ^1.0.3
  tray_manager: ^0.5.3
  window_manager: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/brand/
    - assets/tray/
